
<h1 class="text-center mt-4 mb-4">Saved Setup Sheets</h1>
<%= link_to 'Create new setup', new_setup_path, class: "btn btn-outline-secondary btn-sm mt-2 d-grid gap-2 col-4 mx-auto" %>

<div class="flickr_pagination">
      <%= will_paginate @setups, :continer => false %>
</div>

<%= render 'setup' %>

<div class="flickr_pagination mb-4">
      <%= will_paginate @setups, :continer => false %>
</div>